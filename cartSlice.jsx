import { createSlice } from "@reduxjs/toolkit";
import message from "antd";

const cartSlice=createSlice({
    name:"mycart",
    initialState:{
        cart:[]
    },
    reducers:{
        addtocart:
    }
})